--- ../src_base/minecraft_server/cpw/mods/fml/common/toposort/ModSorter.java	0000-00-00 00:00:00.000000000 -0000
+++ ../src/minecraft_server/cpw/mods/fml/common/toposort/ModSorter.java	0000-00-00 00:00:00.000000000 -0000
@@ -73,7 +73,9 @@
                 else
                 {
                     modGraph.addEdge(before, mod);
-                    if (nameLookup.containsKey(dep)) {
+
+                    if (nameLookup.containsKey(dep))
+                    {
                         modGraph.addEdge(nameLookup.get(dep), mod);
                     }
                 }
@@ -93,7 +95,9 @@
                 else
                 {
                     modGraph.addEdge(mod, after);
-                    if (nameLookup.containsKey(dep)) {
+
+                    if (nameLookup.containsKey(dep))
+                    {
                         modGraph.addEdge(mod, nameLookup.get(dep));
                     }
                 }
